/*
 * Initial main.c file generated by Glade. Edit as required.
 * Glade will not overwrite this file.
 */

#ifdef HAVE_CONFIG_H
#  include <config.h>
#endif

#include <gtk/gtk.h>

#include "interface.h"
#include "support.h"

int
main (int argc, char *argv[])
{
  GtkWidget *window_login;
  GtkWidget *window_employee;
  GtkWidget *gestion_des_plantations;
  GtkWidget *window_commentaire;
  GtkWidget *window_add;
  GtkWidget *window_edit;
  GtkWidget *window_delete;
  GtkWidget *dialog2;

#ifdef ENABLE_NLS
  bindtextdomain (GETTEXT_PACKAGE, PACKAGE_LOCALE_DIR);
  bind_textdomain_codeset (GETTEXT_PACKAGE, "UTF-8");
  textdomain (GETTEXT_PACKAGE);
#endif

  gtk_set_locale ();
  gtk_init (&argc, &argv);

  add_pixmap_directory (PACKAGE_DATA_DIR "/" PACKAGE "/pixmaps");

  /*
   * The following code was added by Glade to create one of each component
   * (except popup menus), just so that you see something after building
   * the project. Delete any components that you don't want shown initially.
   */
  //window_login = create_window_login ();
 // gtk_widget_show (window_login);
  //window_employee = create_window_employee ();
  //gtk_widget_show (window_employee);
  gestion_des_plantations = create_gestion_des_plantations ();
  gtk_widget_show (gestion_des_plantations);
  /*window_commentaire = create_window_commentaire ();
  gtk_widget_show (window_commentaire);
  window_add = create_window_add ();
  gtk_widget_show (window_add);
  window_edit = create_window_edit ();
  gtk_widget_show (window_edit);
  window_delete = create_window_delete ();
  gtk_widget_show (window_delete);
  dialog2 = create_dialog2 ();
  gtk_widget_show (dialog2);*/

  gtk_main ();
  return 0;
}

